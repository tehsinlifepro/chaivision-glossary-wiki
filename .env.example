# Copy to .env and set your own passcode. This value encrypts the glossary
# payload at build time; visitors must type it to unlock/decrypt the wiki.
# Changing it requires a rebuild + push, and invalidates the old passcode.
WIKI_PASSCODE=change-me
